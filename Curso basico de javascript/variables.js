/* Es la manera en la que vamos a guardar nuestros valores. representa el lugar donde vamos a guardar valores */

/* A continuacion se muestra la sintaxis para declarar una variable */
var nombre = "yefry"; 

/* Hay 2 estados de una variable */

/* 1. Declarar una variable. Aqui generamos la palabra var y le colocamos un nombre para guardarlo en la memoria*/
var edad;
/* 2. Inicializar la variable. Se llama asi porque al ya darle un valor, ya la puedo usar. Aqui mandamos a llamar el nombre de la variable y le colocamos un valor*/
edad = 30; 

/* Tambien podemos generar variables de arrays */
var elementos = ["computadora", "celular"];

/* Tambien podemos generar variables tipo objeto */
var persona = {
    nombre: "Yefry",
    edad: 30
}

/* cabe destacar que despues de crear la variable, si la necesitaramos solo usariammos el nombre de la variable, pues al crearla ya la guardamos */
